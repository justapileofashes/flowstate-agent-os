# --- server-only (never expose) ---
# Used for the waitlist, contact form, and release/download tables.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# --- public (safe to expose) ---
NEXT_PUBLIC_SITE_URL=https://flowstate.app
